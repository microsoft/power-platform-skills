# Functional tests for mobile-app scripts and template contracts.
name: mobile-apps-script-tests

on:
    pull_request:
        branches:
            - main
        paths:
            - "plugins/mobile-apps/scripts/**"
            - "plugins/mobile-apps/skills/create-mobile-app/SKILL.md"
            - "plugins/mobile-apps/skills/debug-app/SKILL.md"
            - ".github/workflows/mobile-apps-script-tests.yml"

jobs:
    test-mobile-app-scripts:
        name: test-mobile-app-scripts (${{ matrix.os }})
        runs-on: ${{ matrix.os }}
        strategy:
            fail-fast: false
            matrix:
                os:
                    - ubuntu-latest
                    - windows-latest
                    - macos-latest
        steps:
            - name: checkout
              uses: actions/checkout@v4

            - name: setup-node
              uses: actions/setup-node@v4
              with:
                  node-version: 22

            - name: syntax-check-mobile-template
              shell: bash
              run: node --check plugins/mobile-apps/template/metro.config.js

            - name: run-mobile-script-tests
              shell: bash
              run: node --test plugins/mobile-apps/scripts/tests/*.test.js
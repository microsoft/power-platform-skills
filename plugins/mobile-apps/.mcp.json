{
  "$schema": "https://modelcontextprotocol.io/schema/mcp-server.json",
  "_comment": "Optional MCP servers shipped with the mobile-app plugin. Expo MCP targets a running Metro dev server for native screenshots, view-tree automation, and app logs used by /visual-qa; /debug-app remains terminal-log focused. Microsoft Learn provides authoritative Microsoft platform documentation.",
  "mcpServers": {
    "expo": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "expo-mcp",
        "--dev-server-url",
        "http://localhost:8081"
      ]
    },
    "microsoft-learn": {
      "type": "http",
      "url": "https://learn.microsoft.com/api/mcp"
    }
  }
}

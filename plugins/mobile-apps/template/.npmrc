# Registry — all packages (public and private) are served through the
# Azure Artifacts feed. The feed must have the npm public registry configured
# as an upstream source (enabled by default on new feeds).
registry=https://msazure.pkgs.visualstudio.com/OneAgile/_packaging/mobile-codegen-expo/npm/registry/
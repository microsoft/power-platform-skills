{
  "name": "power-platform-skills-mobile-apps",
  "version": "0.2.0",
  "private": true,
  "description": "Test-time dependencies for the mobile-apps plugin scripts. At runtime the semantic validator prefers the validated app's own TypeScript; this devDependency is only a development/CI fallback.",
  "license": "MIT",
  "scripts": {
    "test": "node scripts/run-tests.js"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}

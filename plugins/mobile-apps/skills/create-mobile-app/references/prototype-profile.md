# Local-data profile compatibility reference

The internal `--prototype` flag now selects the main-based
[phone app profile](phone-app-profile.md). Follow that profile, not the former
Product Experience compiler workflow. Runtime file names and protocol operation
IDs remain compatible with the existing Player bridge.

# Fixture 5-kanban-task-board — Real Claude capture

**Status:** Real `/genpage` capture (2026-05-21). Has known-failing
assertions on Layer 1 (workflow-log compactness — pre-v2.2 spec) and
on Layer 2 (Rule 11 queryTable `.rows` access — pre-Rule-11 docs ingestion).
See "Known gaps" below.

## Source

Captured from a real `/genpage` session against
`https://aurorabapenv610b3.crm.dynamics.com`. Driven by `claude-sonnet-4-6`
under the v2.1 plugin + v2.2 Phase 0.5 manifest generator + v2.2 sample 11
(`samples/11-kanban-with-dnd.tsx`). Working dir was `D:/temp/task-board/`.

Prompt was the eval 5 prompt verbatim:

> Build a task management board with columns for To Do, In Progress, and
> Done. Use the task entity. Allow dragging tasks between columns.

The planner correctly:
- Detected `task` as an existing Dataverse entity (no creation required)
- Selected `samples/11-kanban-with-dnd.tsx` as the reference sample
  (validates that sample 11 from v2.2 is being picked up by the planner)
- Verified statuscode/statecode enum values from `RuntimeTypes.ts` before
  generating the column mapping (Not Started=2, In Progress=3, Completed=5)

## What this capture validates

- **Layer 2 nearly clean** — every code-gen rule passed except the
  v2.2-added Rule 11 assertion (queryTable result must be accessed via
  `.rows`). The captured code does `setTasks(result)` where `result` is
  a `DataTable<T>`, not an array. Generated before Jason's queryTable
  docs (commit `3dc1686`) were fully ingested by the agent, and Rule 11
  wasn't a Layer 2 assertion until v2.2.
- **Sample 11 (v2.2) integrates correctly** — the planner picked it as the
  reference; the generated `task-board.tsx` followed the kanban + DnD
  pattern faithfully.
- **Phase 0.5 (manifest generator) integrates correctly** — the run
  recorded "Files written: package.json, genpage.d.ts" and the capture
  script correctly skipped both as local-dev scaffolding.

## Known gaps (correctly flagged by the runner)

The Layer 1 failures are the same workflow-log compactness issues observed
in `2-mock-dashboard-real/` — the captured workflow-log is in
decision/outcome-summary style rather than command-verbatim style.

| Failing assertion | Why |
|---|---|
| `node --version + pac help separately + version verified` | Log records `Auth: <user>` (the outcome) but not the prereq commands |
| `pac auth list reported` | Same — outcome present, command not |
| `Question 1 (new or edit) via AskUserQuestion` | Log says `Plan written` without recording the AskUserQuestion |
| `Plan presented via EnterPlanMode` | Log says `Plan written` without recording EnterPlanMode |
| `Phase 6 upload --prompt scoping` | Log records `Command: pac model genpage upload --add-to-sitemap --data-sources "task"` without `--prompt` and other flags |

This capture was run on the same day the v2.2 planner-spec tightening
landed (see `agents/genpage-planner.md` and `skills/genpage/SKILL.md`
Phase 6 + Phase 8 commits). The agent session producing this run was
operating under the **older spec** that did not require command-verbatim
logging. A fresh `/genpage` run after the plugin reloads should produce a
log that satisfies all five assertions.

## Known gaps — Layer 2

| Failing assertion | Why |
|---|---|
| Rule 11: queryTable result must be accessed via `.rows` | Captured code does `setTasks(result)` directly on the DataTable wrapper |

## How to re-capture (recommended)

Re-capture under the v2.2 spec to flip both layers green in one go — the
new planner spec records command-verbatim logs (Layer 1) and the agent
will follow Rule 11 for queryTable (Layer 2). To re-capture:

1. **Start a fresh `claude` session** so the plugin reloads with the new
   spec.
2. **Run `/genpage`** with eval 5's prompt verbatim:

   > Build a task management board with columns for To Do, In Progress,
   > and Done. Use the task entity. Allow dragging tasks between columns.

   Answers: data source = task entity; pick any existing app.

3. **Capture and verify in one step:**

   ```bash
   node plugins/model-apps/scripts/capture-fixture.js \
     --working-dir <path-to-new-working-dir> \
     --eval 5 \
     --slug kanban-task-board \
     --force
   ```

   When both layers report empty `failures` arrays, **delete this README**.

## Files

| File | Source |
|------|--------|
| `task-board.tsx` | Generated by `/genpage` page-builder phase |
| `RuntimeTypes.ts` | Generated by `pac model genpage generate-types --data-sources task` |
| `workflow-log.md` | Recorded during the run |
| `genpage-plan.md` | Written by `genpage-planner` agent |
